# Runbook — Carried-forward test-coverage debt

When the verify stage rates a coverage gap as a **non-blocking follow-up** — a
missing test that does not block the current merge but leaves a real contract
under-covered — that finding must be recorded here rather than left in a PR
review that disappears from view once the PR merges. Otherwise the same gap is
rediscovered and re-litigated every time the code path is touched.

This is the coverage-debt analogue of `smoke-verification.md`'s deferred-smoke
table. It is intentionally *not* for blockers: a blocker keeps a PR from
merging and is fixed in that PR. This ledger is for gaps that are acceptable to
carry but should be tracked to closure.

## Adding a row

The verify stage appends a row whenever it accepts a coverage gap as a
non-blocking follow-up (see `.claude/skills/verify/SKILL.md` §6). Include the
issue/PR where it surfaced, the exact contract left uncovered, and the file
that would hold the test.

## Clearing a row

Close a row by adding the missing test (in its own change or folded into a
later issue that touches the path), then mark it Done with the PR that closed
it — or delete the row once that PR records it. A planned issue that will
naturally cover the path should reference the relevant open rows.

## Open test-coverage debt

| First seen | Also hit by | Uncovered contract | Where a test belongs | Status |
|---|---|---|---|---|
| #5 (PR #23) | #7 (PR #27) | `CraftConfirmDialog`'s Android hardware-back (`BackHandler` / `hardwareBackPress`) cancel path has no automated coverage anywhere in the repo; the confirmed-reset and confirmed-delete flows both rely on it | `tests/` — a `CraftConfirmDialog` (or dialog-hosting screen) test that simulates `hardwareBackPress` and asserts the dialog cancels without performing the destructive action | Open |
| #13 (PR #39) | — | AC5/NFR-12 logging hygiene is proven only as a proxy: `tests/loggingHygiene.test.ts` asserts no `console.*` in `src/`, but cannot prove a *future* code/context-only logging seam excludes maker content (pattern text, notes, transcript excerpts) from its payload — no such seam exists yet to test against | `tests/` — when a diagnostic-logging seam is introduced, a contract test asserting the seam's log payload never contains injected maker-content fields (pattern text, notes, transcript excerpts) | Open |
| #14 (PR #41) | — | `tests/accessibilityContrast.test.ts` does not see a token background carried on a **non-`CraftPressable` ancestor** paired with text in a **separate string literal**; the pairing rules cover same-literal classes and `CraftPressable` spans only (verify probe: `text-surface` on the white step card — white on white — passed 9/9) | `tests/accessibilityContrast.test.ts` — an ancestor-background span rule, or nearest-ancestor background resolution | Open |
| #14 (PR #41) | — | iOS `useAnnouncement` wiring is untested on `DictionaryScreen`, `StitchDetailScreen`, `PatternsScreen`, `GuidesScreen`, `PatternEditorScreen`, `GuideEditorScreen`, `GuideVideoPlayer`, `DatabaseGate`; typecheck constrains the constants but a wrong hook condition is silent with no test failing (the import screen was proven by an uncommitted review probe) | one `announceForAccessibility` case per surface in that surface's existing suite — the committed `PatternViewerScreen`/`GuideWorkingViewScreen` iOS cases are the template | Open |
| #43 (PR #49) | — | Every #43 test asserted chrome *containment* via `within(list)` but nothing pinned the header's **order** — title → progress → video → counter → steps — which is the whole substance of the recorded UX-06 trade-off ("+" reachable with one flick and without scrolling past the video). A reorder putting the counter above the video, or a header passed as `ListFooterComponent`, passed all seven new cases and the router case | `tests/GuideWorkingViewScreen.test.tsx`, "one scroll surface (issue #43)" block — a relative-position assertion over the walked subtree | **Done** — closed by the #43 retro PR, which added "keeps the chrome in order" and confirmed it goes red (and only it) under a video/counter swap |
| #42 (PR #48) | — | `tests/CraftTextField.test.tsx` T1 did not assert the **single-line** surface keeps `items-center`. The single-line input's own padded box already fills the 48px, so `items-center` is not load-bearing for the text — but it is what centres the leading icon and the clear control against the input, and switching the surface to `items-start` left all eight tests green | `tests/CraftTextField.test.tsx` T1 — a `className` match on the surface, beside the existing `toHaveStyle({ minHeight: 48 })` | **Done** — closed by the #42 retro PR, which added the assertion and confirmed it goes red (and only it) under an `items-start` mutation |
| #46 (PR #55) | — | A future component that renders stroked SVG art **without going through `StitchStepAnimation`**, with its colour **imported from another module**, is seen by neither guard: the file-level raw-hex walk never arms (an import carries no hex into the file that strokes with it) and the literal-palette pin reads only `StitchStepAnimation`'s own rendered output | `tests/accessibilityContrast.test.ts` — a walk asserting every `stroke=`/`fill=` value in `src/` resolves to `STROKE_COLOR[…]`, `tokens.colors.*`, or `"none"` | **Done** — closed by the #46 retro PR, which added "resolves every stroke and fill in src/ through the measured palette" plus its non-tautology, and confirmed it is the only test red under exactly that probe (a `.ts` sibling exporting `#FFD166`, imported onto a `<Path stroke={…}>` in a new `.tsx`); both pre-existing rules stayed green, so the gap was real |
| #46 (PR #55) | — | Verify's first review found two gaps in the same PR — the per-step stagger (`stepIndex × tokens.motion.stepStaggerMs`, with every delay forced to `0` leaving all suites green) and the drawing rendering **below** its instruction sentence | `tests/StitchStepAnimation.test.tsx` and `tests/StitchDetailScreen.test.tsx` | **Done — closed in-cycle**, not carried: the build's fix run added both tests, and the re-verify's probes A and B turned each red. Recorded here so the ledger shows they were closed rather than dropped |
| #56 (PR #60) | — | `PatternViewerHeader` took ownership of the counter's `status === 'failed'` alert ("Try again to load the counter"), but no test on this screen renders that state at all, so nothing pinned the alert inside `pattern-steps` — a revert that left it as a sibling above the list would have stayed green | `tests/PatternViewerScreen.test.tsx`, "one scroll surface (issue #56)" block — render with a `counters.getOrCreatePrimaryCounter` that throws and assert the alert and its retry are inside the list | **Done** — closed by the #56 retro PR, which added "keeps the counter read-failure alert inside the step list" and confirmed it is the only test red when the failed branch is moved out of the header to a sibling above the list |
| #44 (PR #54) | — | Plan §6 states the whole seed release is one transaction and that a mid-loop failure leaves no partial pattern; no committed test pinned it. Verify confirmed the behaviour with a scratch probe (record 3 given a `NULL` title after two complete records) that was discarded with its review worktree | `tests/patternSeedLoader.test.ts` — a mid-loop failure case asserting `pattern`, `pattern_step`, and `pattern_seed_state` all roll back, the applied version stays `undefined`, and a retry seeds the full set | **Done** — closed by the #44 retro PR, which added "rolls the whole release back when a record fails part-way through" and confirmed it is the only test red when the `transaction(...)` wrapper is removed from `insertSeededPatterns` |
| #50 (PR #61) | — | `scanEntries`' `closed` flag was unpinned: a blank line must close the open entry so later prose belongs to no entry. Verify probe P1 dropped the check and every suite stayed green — a sponsor blurb between two chapter lines would have shipped inside the step above it | `tests/pastedGuideSteps.test.ts` — `0:00 Materials\n\nSponsored by Yarn Co\n1:12 Magic ring` must yield instructions `['Materials', 'Magic ring']` | **Done** — closed by the #50 retro PR ("drops prose that a blank line cut off from the entry above it"), confirmed the only test red under P1 |
| #50 (PR #61) | — | The cue-run break on a **non-ascending** offset was pinned only by a strictly descending fixture, which cannot tell `<=` from `<`. Verify probe P2 changed `entry.offsetMs <= open.lastOffsetMs` to `<` and every suite stayed green, so two cues sharing an offset silently merged | `tests/pastedGuideSteps.test.ts` — `0:00 a\n0:03 b\n0:03 c\n0:06 d` must yield two blocks at `[0, 3000]` | **Done** — closed by the #50 retro PR ("breaks a cue run when two consecutive cues share an offset"), confirmed the only test red under P2 |
| #51 (PR #64) | the four sibling focus-refresh screens | The `useFocusEffect(useCallback(…, [refresh]))` dependency array was unfalsified on **all five** screens that re-read persisted data. The build's M13 widened it to `[refresh, state]` and every suite stayed green — the isolated screen suites mock `useFocusEffect` as a capture, so a re-subscription loop is invisible to them by construction. Re-verify showed the real-router read count climbing under M13 | a real-router suite that counts repository reads, settles the tree, and asserts the count is stable | **Done** — closed by the #51 retro PR, which added `tests/focusReadBudget.test.tsx` (one case per screen) over `tests/support/countedRepositories.ts`, and confirmed each case is red under M13 applied to **its own** screen and green under M13 applied to the other four |
| #51 (PR #64) | the shipped `/patterns/new` create form | A repeated tap on a create-style confirm before `router.replace` lands writes a **second** aggregate. Re-measured by the retro on `main`: two presses of the shipped "Create pattern" give a `listPatterns()` delta of **2**; the #51 review screen has the same exposure. Pre-existing, so not a #64 blocker | `tests/patternsNavigation.test.tsx` and `tests/guidesNavigation.test.tsx` (real router; the isolated suites cannot see the still-mounted-after-replace state) | Open — filed as [#72](https://github.com/ciaran-slow/MellowMaker/issues/72), which owns both the guard and these tests |
| #50 (PR #61) | — | The classifier's "every entry labelled" condition was unpinned and is **load-bearing**: verify probe P3 dropped `validateGuideStepInstruction(entry.text).ok` and every suite stayed green, so `0:00 Materials\n1:12\n2:40 Round 1` classified as chapters and persisted a step with `instruction: ''` — `guide_step.instruction` is `NOT NULL` but carries no non-empty `CHECK` | `tests/pastedGuideSteps.test.ts` — that input must classify as `cues` and yield two steps at `[0, 160000]` | **Done** — closed by the #50 retro PR ("falls back to cues when any entry carries no label"), confirmed the only test red under P3. The missing schema `CHECK` behind it shipped in #67: `pattern_step.instruction` and `guide_step.instruction` now carry a named non-empty `CHECK` at schema version 3, so an empty instruction is refused by the database whatever the classifier does |
| #66 (PR #69) | — | The repeat contract was migrated to **six** surfaces and pinned at **two**. Verify's M-F/M-G/M-H/M-K each deleted one surface's `setAttempt((n) => n + 1)` — `PatternStepEditorRow.saveEdit`, `UrlEntryForm.submit`, `GuideStepEditorRow.saveEdit`, `AddStepField.submit` — and the whole suite stayed green, so a partial revert at any of the four restores the dead tap undetected | `tests/PatternStepEditorRow.test.tsx`, `tests/GuideImportScreen.test.tsx`, `tests/GuideEditorScreen.test.tsx` (step row), `tests/PatternEditorScreen.test.tsx` — the "tap the same failing control twice → two announcements, identity advanced" pair at each | **Done** — closed by the #66 retro PR, which added the repeat, negative-branch and Android-identity cases at all four surfaces and confirmed each surface's block is red under that surface's own bump deletion and green under the other three |
| #66 (PR #69) | — | The import surface counted no utterances. Verify's M-J re-added the parent screen's deleted `useAnnouncement(urlError)` — the exact iOS double-speak the conversion existed to avoid — and every suite stayed green. Pre-existing (no announcement assertion on this screen on base either), but #69 moved the mechanism into the child, so a regression would be silent | `tests/GuideImportScreen.test.tsx` — one `announceForAccessibility` call per rejection, asserted as an exact count | **Done** — closed by the #66 retro PR ("speaks a rejected link exactly once, from the field and not the screen"), confirmed red under M-J |
| #66 (PR #69) | — | The walk guard read `accessibilityRole` only as a literal in a `<Text>` opening tag. `CraftAnnouncement` spread the prop onto its live-region `Text`, so `<CraftAnnouncement accessibilityRole="alert" politeness="assertive">` reproduced the banned shape at runtime, without an `attempt`, and passed `inlineErrorPrimitive` | `tests/inlineErrorPrimitive.test.tsx` — a matcher for the indirect carriers, or this ledger if judged not worth one | **Done** — closed by the #66 retro PR: the unused `accessibilityRole` prop was deleted from `CraftAnnouncement` (no caller ever passed it), and the guard now also bans a props spread and a non-literal `accessibilityRole` on any live-region `Text` |
| #67 (PR #71) | — | `insertSeededPatterns` is one of the eight writers wrapped by `withStepInstructionGuard`, and the only one with no test driving an empty instruction through it to `empty-step-instruction`. The bundled document cannot produce one (`parsePatternSeedDocument` refuses it upstream), so the branch was reachable only by a hand-built record — the same shape the mid-loop rollback case already uses | `tests/patternSeedLoader.test.ts` — a record with a whitespace-only step, asserting the typed code and that the whole release rolls back | **Done** — closed by the #67 retro PR, which added "refuses a seeded record whose step instruction is whitespace, and rolls the release back" and confirmed it is the only test in that suite red when the `pattern_step` `CHECK` is dropped from the version-3 rebuild |
